# Immutable
# order maintain
# you can use any datatype in a particular variable

trip_summary = ("ubergo", "coimbatore","airport",450.00,"completed","completed")

print(trip_summary)
print(trip_summary[1])

print(len(trip_summary))
print(trip_summary.count("completed"))

# print(trip_summary[1]) = "karur" because it is immutable

